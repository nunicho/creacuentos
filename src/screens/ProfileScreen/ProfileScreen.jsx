import { StyleSheet, Text, View } from "react-native";

import React from "react";

const ProfileScreen = () => {
  return (
    <View>
      <Text>Perfil del usuario, aquí podrá configurar las opciones como nombre, edad, color preferido etc </Text>
    </View>
  );
};

export default ProfileScreen;

const styles = StyleSheet.create({});
